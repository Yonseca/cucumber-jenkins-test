pipeline {
    agent any
    stages {
        stage('Test') {
            steps {
                mvn test
            }
        }
    }
    post {
        always {
            junit 'target/cucumber/*.xml'
        }
    }
}